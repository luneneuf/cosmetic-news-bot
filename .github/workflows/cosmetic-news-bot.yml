name: cosmetic-news-bot

on:
  schedule:
    # KST 09:00-17:45 평일 매 15분 (public repo라 Actions 한도 무제한)
    # UTC 00:00-08:45 월-금
    - cron: '*/15 0-8 * * 1-5'
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: cosmetic-news-bot
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Restore state cache
        id: cache-restore
        uses: actions/cache/restore@v4
        with:
          path: |
            seen_links.json
            seen_titles.json
            seen_titles_embeddings.json
            posted_log.jsonl
          # v6: posted_log.jsonl 추가 (아침 브리핑 봇 데이터 소스)
          key: news-bot-seen-v6-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            news-bot-seen-v6-

      - name: Init empty state if cache miss
        run: |
          for f in seen_links.json seen_titles.json seen_titles_embeddings.json; do
            if [ ! -f "$f" ]; then
              echo '[]' > "$f"
              echo "$f: initialized empty"
            else
              echo "$f: $(wc -c < "$f") bytes"
            fi
          done
          if [ ! -f "posted_log.jsonl" ]; then
            touch posted_log.jsonl
            echo "posted_log.jsonl: initialized empty"
          else
            echo "posted_log.jsonl: $(wc -l < posted_log.jsonl) lines"
          fi

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: requirements.txt

      - run: pip install -r requirements.txt

      - name: Collect and post
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python collect_and_post.py

      - name: Save state cache
        if: always()  # 워크플로 fail에도 save 보장
        uses: actions/cache/save@v4
        with:
          path: |
            seen_links.json
            seen_titles.json
            seen_titles_embeddings.json
            posted_log.jsonl
          key: news-bot-seen-v6-${{ github.run_id }}-${{ github.run_attempt }}
